# LMS


## TODO
```composer install``` <br>
```npm install``` <br>
```cp .env.example .env```<br>
```php artisan key:generate```<br>
```php artisan migrate --seed``` <br>
```npm run dev``` <br>
```php artisan serve```